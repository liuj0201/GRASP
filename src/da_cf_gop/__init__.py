"""DA-CF-GoP research pipeline.

The package is deliberately independent of the historical ``phoneme_project``
implementations.  Historical files may be migrated with provenance, but they
are never imported at runtime.
"""

from __future__ import annotations

__version__ = "0.1.0"

